services:
  nginx:
    image: nginx:1.31-alpine
    container_name: drafftink-nginx
    depends_on:
      - drafftink-server
      - drafftink-web
    ports:
      - "8000:8000"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
